

# Sudoku
  
to start the program: 
- npm install
- node main.js 


- the program first ask for the size of the array where if the array is 4x4 then the size to insert is 4 
- 3 arrays that correspond to  a list of column, box and raw array which each contain the set values for the specified location are generated and cells are initiaed

- you can keep adding/updateing values in the sudoku untill you write finish and that will display the final result